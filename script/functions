info() {
  echo "[INFO] $@"
}

smitty() {
  echo "[RUN] $@"
  "$@"
}

err() {
  echo "[ERROR] $@" >&2
  exit 1
}

logger() {
  docker run --rm --volumes-from rsyslog gliderlabs/alpine:latest logger $@
}
